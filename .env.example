# Database PostgreSQL (Railway fornisce questa variabile automaticamente)
DATABASE_URL="postgresql://user:password@host:5432/dbname"

# NextAuth - genera un segreto con: openssl rand -base64 32
NEXTAUTH_SECRET="cambia-questo-con-un-segreto-sicuro"

# URL del tuo deployment (Railway lo imposta automaticamente)
NEXTAUTH_URL="https://tua-app.railway.app"
